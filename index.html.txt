



<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Muse Atelier - Beyond Ordinary</title>
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Cormorant+Garamond:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

```
    html {
        background-color: #3d1212 !important;
    }
    
    body {
        font-family: 'Cormorant Garamond', 'Georgia', serif;
        background-color: #3d1212 !important;
        color: #f5f5f5;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px 20px;
    }
    
    .container {
        max-width: 1100px;
        width: 100%;
        text-align: center;
        background-color: #3d1212;
    }
    
    .hero-image {
        max-width: 500px;
        width: 100%;
        margin: 0 auto 3.5rem;
        background-color: transparent;
    }
    
    .hero-image img {
        width: 100%;
        height: auto;
        display: block;
        max-width: 100%;
    }
    
    h1 {
        font-family: 'Great Vibes', cursive;
        font-size: 6rem;
        font-weight: 400;
        margin-bottom: 2rem;
        line-height: 1.2;
        letter-spacing: 3px;
        color: #ffffff;
    }
    
    .subtitle {
        font-size: 1.1rem;
        font-weight: 300;
        margin-bottom: 1.5rem;
        letter-spacing: 3px;
        opacity: 0.95;
        text-transform: uppercase;
        font-size: 0.95rem;
        color: #f5f5f5;
    }
    
    .description {
        font-size: 1.2rem;
        margin-bottom: 3.5rem;
        max-width: 650px;
        margin-left: auto;
        margin-right: auto;
        line-height: 1.8;
        opacity: 0.9;
        font-weight: 300;
        color: #f5f5f5;
    }
    
    .email-form {
        max-width: 520px;
        margin: 0 auto;
        display: flex;
        gap: 12px;
        flex-direction: column;
    }
    
    input[type="email"] {
        padding: 22px 30px;
        font-size: 1.05rem;
        border: 1.5px solid rgba(255, 255, 255, 0.4);
        background: rgba(255, 255, 255, 0.08);
        color: #fff;
        font-family: 'Cormorant Garamond', serif;
        letter-spacing: 1px;
        transition: all 0.3s;
        text-align: center;
    }
    
    input[type="email"]::placeholder {
        color: rgba(255, 255, 255, 0.55);
    }
    
    input[type="email"]:focus {
        outline: none;
        border-color: rgba(255, 255, 255, 0.7);
        background: rgba(255, 255, 255, 0.12);
    }
    
    button {
        padding: 22px 50px;
        font-size: 0.95rem;
        background: #ffffff;
        color: #3d1212;
        border: none;
        cursor: pointer;
        font-family: 'Cormorant Garamond', serif;
        letter-spacing: 2.5px;
        font-weight: 600;
        transition: all 0.3s;
        text-transform: uppercase;
    }
    
    button:hover {
        background: #f8f8f8;
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
    }
    
    .success-message {
        display: none;
        margin-top: 25px;
        padding: 20px;
        background: rgba(255, 255, 255, 0.12);
        border: 1.5px solid rgba(255, 255, 255, 0.35);
        font-size: 1.05rem;
        letter-spacing: 1px;
        color: #ffffff;
    }
    
    .social {
        margin-top: 4rem;
        font-size: 1rem;
        letter-spacing: 2px;
    }
    
    .social a {
        color: #ffffff;
        text-decoration: none;
        opacity: 0.75;
        transition: opacity 0.3s;
    }
    
    .social a:hover {
        opacity: 1;
    }
    
    @media (max-width: 768px) {
        h1 {
            font-size: 3.8rem;
        }
        
        .subtitle {
            font-size: 0.85rem;
            letter-spacing: 2px;
        }
        
        .description {
            font-size: 1.05rem;
        }
        
        .hero-image {
            max-width: 350px;
            margin-bottom: 3rem;
        }
        
        input[type="email"],
        button {
            padding: 20px 28px;
            font-size: 1rem;
        }
    }
</style>
```

</head>
<body>
    <div class="container">
        <div class="hero-image">
            <img src="https://i.imgur.com/cYbgxHv.jpeg" alt="Muse Atelier" crossorigin="anonymous">
        </div>

```
    <h1>Beyond Ordinary</h1>
    
    <p class="subtitle">Luxury Pilates • Elevated Café • Curated Lifestyle</p>
    
    <p class="description">
        Muse Atelier is redefining wellness. Join our founding members and be part of something extraordinary.
    </p>
    
    <form class="email-form" id="emailForm">
        <input 
            type="email" 
            id="emailInput"
            placeholder="Enter your email" 
            required
        >
        <button type="submit">Be Among the First</button>
    </form>
    
    <div class="success-message" id="successMessage">
        ✓ Thank you! You're on the list. Check your email soon.
    </div>
    
    <div class="social">
        <a href="https://instagram.com/museatelier.co" target="_blank">@museatelier.co</a>
    </div>
</div>

<script>
    document.getElementById('emailForm').addEventListener('submit', function(e) {
        e.preventDefault();
        
        const email = document.getElementById('emailInput').value;
        
        // Store email for this session
        const emails = JSON.parse(sessionStorage.getItem('museEmails') || '[]');
        emails.push({
            email: email,
            timestamp: new Date().toISOString()
        });
        sessionStorage.setItem('museEmails', JSON.stringify(emails));
        
        // Show success message
        document.getElementById('successMessage').style.display = 'block';
        document.getElementById('emailInput').value = '';
        
        // Hide after 5 seconds
        setTimeout(() => {
            document.getElementById('successMessage').style.display = 'none';
        }, 5000);
        
        console.log('Email submitted:', email);
    });
</script>
```

</body>
</html>